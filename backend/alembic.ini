[alembic]
script_location = alembic
prepend_sys_path = .
# The connection URL is not set here on purpose: alembic/env.py reads it from
# DATABASE_URL so that migrations and the application can never disagree, and
# so no credentials live in a tracked file.
